<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#FFFFFF"
    android:orientation="vertical"
    android:paddingTop="16dp"
    android:paddingBottom="8dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingBottom="4dp">

        <TextView
            android:id="@+id/tv_topic_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_toStartOf="@id/tv_topic_see_more"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Tiêu đề chủ đề"
            android:textColor="#F24405"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_topic_see_more"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?android:attr/selectableItemBackground"
            android:paddingHorizontal="4dp"
            android:paddingVertical="4dp"
            android:text="Xem thêm ›"
            android:textColor="#757575"
            android:textSize="13sp" />
    </RelativeLayout>

    <TextView
        android:id="@+id/tv_topic_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingBottom="10dp"
        android:text="Mô tả ngắn về chủ đề"
        android:textColor="#757575"
        android:textSize="13sp" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_topic_stores"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:overScrollMode="never"
        android:paddingStart="16dp"
        android:paddingEnd="8dp"
        android:orientation="horizontal"
        tools:listitem="@layout/item_topic_store"
        tools:itemCount="4" />

    <TextView
        android:id="@+id/tv_topic_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Chưa có dữ liệu"
        android:textAlignment="center"
        android:textColor="#9E9E9E"
        android:textSize="14sp"
        android:padding="24dp"
        android:visibility="gone" />
</LinearLayout>
