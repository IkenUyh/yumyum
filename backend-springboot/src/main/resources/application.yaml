spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zalopay_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "123456"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 0
    locations: classpath:db/migration
    out-of-order: false
    table: flyway_schema_history

server:
  port: 8081

cloudinary:
  cloud-name: "dkm39ilz8"
  api-key: "392638882274686"
  api-secret: "M4row-OumipTXq-Nqn8mUdZMs4Y"